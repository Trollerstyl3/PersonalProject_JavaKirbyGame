/* Name: Khoi Nguyen
 * Date: November 20, 2020
 * Class Description: This enum class contains all the state that the character can be in
 */

// Please don't remove the packages because I have many folders
package src.objects.enums;

// Create the enum class for the character state that the character can have
public enum CharacterState {
    ATTACKING,
    TAUNTING,
    CHARGING,
    JUMPING,
    FALLING,
    SLAM,
    GROUNDED,
    LEFT,
    RIGHT,
    STOPPED,
    HURT,
    DEAD
}
