/* Name: Khoi Nguyen
 * Date: November 20, 2020
 * Class Description: this enum class keeps the type of entity that the game could have
 */

// Please don't remove the packages because I have many folders
package src.objects.enums;

// Create enum class that can have two type of entity which is player and enemy
public enum EntityType {
    ENEMY,
    PLAYER
}
